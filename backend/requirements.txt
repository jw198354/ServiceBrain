# ServiceBrain Backend Dependencies
# Requires Python 3.8+

# FastAPI & Server
fastapi>=0.80.0
uvicorn[standard]>=0.18.0
websockets>=10.0
python-multipart>=0.0.5

# Database
sqlalchemy>=1.4.0,<2.0.0
aiosqlite>=0.17.0

# Pydantic
pydantic>=1.9.0,<2.0.0

# LangChain (simplified for demo)
# Note: Full LangChain requires Python 3.9+
# For Python 3.8, use basic HTTP calls to LLM

# LLM Client
openai>=0.27.0
httpx>=0.23.0

# Utils
python-dotenv>=0.20.0
python-dateutil>=2.8.0
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4

# Testing
pytest>=7.0.0
pytest-asyncio>=0.18.0

# Note: For full LangChain + Chroma support, use Python 3.11+
# See README.md for details
